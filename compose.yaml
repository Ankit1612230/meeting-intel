services:
  postgres:
    image: postgres:16
    container_name: meeting-intel-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: meetingdb
    ports:
      - "5432:5432"